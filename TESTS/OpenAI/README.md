# OpenAI Tests

PDF records and metadata for cold/default and later OpenAI interactions.

Create a subdirectory using the model name as displayed, then a year directory when needed. Preserve interface labels, screenshots, provider statements, and self-identification as distinct forms of model-name evidence.

# Google AI Tests

PDF records and metadata for cold/default and later Google AI interactions.

Create a subdirectory using the model name as displayed, then a year directory when needed. Preserve evidence for model identification in each PDF's metadata.
